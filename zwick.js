function zwick(name = "Zwick") {
    const zap = "⚡ ZPK";
    const wit = "🪝 WIT";
    const lang = "📡 LNG";

    return `${name}: ${zap} | ${wit} | ${lang}`;
}


    return `${name}, du bist offiziell: ${nerd} | ${charm} | ${rums}`;
}
function zwick(name = "Zwick") {
    return `${name}: zwickt kurz, wirkt lang.`;
}
