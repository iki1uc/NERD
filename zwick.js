function nerdCharm(name = "Tomaten-Sammler") {
    const nerd = `🧠 Nerd-Level: mild`;
    const charm = `✨ Charm: hoch`;
    const rums = `💥 Rums: bereit`;

    return `${name}, du bist offiziell: ${nerd} | ${charm} | ${rums}`;
}
